rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Growth media is private. Firebase-authenticated administrators still
    // upload through Functions so content rights, privacy, and audit evidence
    // are validated before the Admin SDK writes an object.
    match /{object=**} {
      allow read, write: if false;
    }
  }
}
